<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ESILib.Views.BookView">
    <Frame BackgroundColor="White" CornerRadius="6" Margin="8" Padding="8" VerticalOptions="Start">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{Binding Coverurl}" Grid.Column="0" HeightRequest="120"/>
            <StackLayout Grid.Column="1" VerticalOptions="CenterAndExpand">
                <Label Text="{Binding Title}" TextColor="Black" FontSize="18" FontAttributes="Bold"/>
                <Label Text="{Binding Author}"/>
            </StackLayout>
        </Grid>
    </Frame>
</ContentView>